// The page connector used in retrieving pages of data
// Default is "local", which directs api's to the 
// [localSeriesData] and [localCardData] of lib/config.ts
export enum CONNECTOR {
  local = 0
}
